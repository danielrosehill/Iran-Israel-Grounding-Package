# Copy to reference/.env. Only OPENROUTER_API_KEY is required for the reference run.
OPENROUTER_API_KEY=
GROUNDING_MODEL=anthropic/claude-sonnet-4.6

# Optional — point at a different grounding-set.md.
# Defaults to ../grounding-set.md relative to this package.
# GROUNDING_SET_PATH=
